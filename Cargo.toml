[package]
name = "layout-switcher"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
signal-hook = "0.3"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"

